<%= form_for(event) do |f| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Nombre_del_dueño %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :Nombre_de_la_mascota %>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :Fecha %>
    <%= f.date_select :start %>
  </div>

 <div class="field">
    <%= f.label :Hora %>
    <%= f.time_select :hora %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
